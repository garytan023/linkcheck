# 日记 | 2026-04-02

> 拉小风视角 · 主控协调日记
> 写入时间：2026-04-02 23:00 (Asia/Shanghai)

---

## 今日概览

今天是 **灾难性的一天**。

下午 Gary 终于问了 "minimax 模型好了么"——老实说，不好。**8个 cron job 全部失败**，根因是 MiniMax-M2.7 在 isolated session 里被 API 直接拒绝（"token plan not supported"）。只有 AI Builders Daily Digest（08:30，main session 执行）侥幸跑通，其余全灭。

更惨的是，主会话（e44b3308）从 08:30 开始被 AI Builders Digest 的同一消息**重复推入 300+ 次**，Gary 的每条消息也被重复 200+ 次——整个系统在那段时间几乎空转。

好消息是：WhatsApp 多次重连后保持住了连接，Gary 在飞书 DM 里也给了明确指令（"只回复 ok"），让交互不至于完全混乱。

---

## 今日亮点

- **✅ AI Builders Daily Digest 执行成功**：08:30 触发，121 秒完成，lastRunStatus ok。这是今天唯一跑通的 cron。
- **✅ WhatsApp 连接保住**：虽然多次重连（17:52/18:14/18:20/22:50），最终保持连线状态。
- **✅ 主会话存活**：e44b3308 虽然空转，但 session 本身没崩，持续运行到深夜。
- **✅ Gary 给了明确指令**：在混乱中 Gary 说"只回复 ok"，这是一次有效的人机协同干预，避免了更严重的消息淹没。
- **✅ 汇总层文件写入**：22:50 本汇总层写入成功，这是本次日记得以产出的前提。

---

## 今日问题

### 🔴 P0 — MiniMax API 在 isolated session 全面故障

**症状**：8个 cron job（daily-memory-refresh、memory-daily-cleanup、ai-news-daily、小风/大风/小知/小数/小投/小创每日日记）**全部失败**，错误均为：
```
FailoverError: HTTP 500 api_error: your current token plan not support model, MiniMax-M2.7 (2061)
```

主会话（main session）通过某种 fallback 机制（切换到 MiniMax-M2.5）仍可运行，但 isolated session 没有这个机制，直接撞墙。

**影响**：
- 今天连续第二天各 agent 日记零产出
- memory-daily-cleanup、ai-news-daily 等基础任务也全部停摆
- 全Agent每日日志中间汇总层（22:50）由本 cron 执行时才真正写入——说明之前那个 22:50 版本可能也失败了

**根因推测**：isolated session 的模型路由/兜底逻辑和 main session 不一致。

---

### 🔴 P0 — 主会话消息队列堆积

08:30 AI Builders Digest 触发后，同一条 Digest 消息被推入主会话 **300+ 次**，Gary 的 "minimax 模型好了么" 也被重复 200+ 次。

这不是 session 本身的问题，而是**飞书消息接收层或消息队列的去重机制失效**。大量重复消息被逐条消费，导致：
- 主会话在 08:30-09:05 完全空转处理垃圾消息
- Gary 被迫淹在自己的消息里

---

### 🟡 P1 — Cron 死锁依赖

22:50 汇总层 cron 依赖它自己生成的汇总层来触发后续各 agent 日记——这是架构设计上的循环依赖。今天 22:50 实际上失败了（本 cron 补跑），说明这个设计是脆弱的。

---

### 🟡 P2 — 飞书群聊内容未沉淀

Gary 在群（oc_85f6128cd8160c22ca0b6efe7da8c11b）里有大量交互，但全部没有记录到任何文件。这些内容对理解 Gary 的实时需求非常有价值，完全流失了。

---

### 🟡 P3 — GROQ Console Exec 被 SIGTERM 中断

b61b6acf session 中一条 exec 命令被系统强制终止（18:34，SIGTERM），GROQ console 注册未完成。

---

## 明天改进

1. **修复 MiniMax isolated session 模型问题**（最高优先级）
   - 方案A：为所有 isolated cron 配置显式 fallback 模型（如 MiniMax-M2.5）
   - 方案B：统一 main session 和 isolated session 的模型兜底逻辑
   - 根本解法可能需要和 Gary 确认 MiniMax token plan 的实际限制

2. **拆解 22:50 cron 死锁**
   - 汇总层 cron 改为 22:00 执行，提前于各 agent 日记 cron
   - 各 agent 日记 cron 直接读取前一天的汇总层，不依赖本轮汇总

3. **飞书消息去重**
   - 检查 openclaw 飞书消息接收层是否有幂等/去重逻辑
   - Gary 发了数百条重复消息这个问题需要先解决，否则任何 cron 都可能被重复触发

4. **群聊内容沉淀**
   - 建议增加一个飞书群消息的定期归档流程（哪怕只是简单的每日摘要写入文件）

---

## 关键指标

| 指标 | 数值 |
|------|------|
| Cron 成功率 | 1/9（~11%） |
| 今日产出文件 | 汇总层 ×1 |
| 各 Agent 日记 | 0（连续第 2 天） |
| 主会话消息堆积 | ~500 条重复消息 |
| 系统可用性 | 降级运行 |

---

*本日记基于 `memory/daily-rollups/2026-04-02.md` 汇总层 + 全量 session 分析生成。*
*如需补充 Gary 群聊具体内容，请查阅 session e44b3308 / b61b6acf transcript。*
