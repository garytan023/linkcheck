

## 今晚补充记忆（配置修复）
- 重新核对大风 / 小风 agent 配置后确认：小风（`dev` → `/Users/garytan/.openclaw/workspace-dev`）正常；大风存在历史错拼写整链路：`assitant` / `workspace-assitant` / `/agents/assitant/agent`，且 Feishu `dafeng` 的 DM 与群路由都绑在这条错误拼写链路上。
- 已执行真实目录迁移，不是只改配置：
  - `/Users/garytan/.openclaw/workspace-assitant` → `/Users/garytan/.openclaw/workspace-assistant`
  - `/Users/garytan/.openclaw/agents/assitant/...` → `/Users/garytan/.openclaw/agents/assistant/...`
  - 同时把嵌套残留的 `agent/` 与 `sessions/` 目录整理到正确位置。
- 已修正 OpenClaw 配置中的大风标识与路由：
  - agent `id` 从 `assitant` 改为 `assistant`
  - `bindings[*].agentId` 中大风路由从 `assitant` 改为 `assistant`
  - workspace 改为 `/Users/garytan/.openclaw/workspace-assistant`
  - agentDir 改为 `/Users/garytan/.openclaw/agents/assistant/agent`
- 已彻底清理 MiniMax-M2.5 遗留：
  - `models.providers.minimax-cn.models` 只保留 `MiniMax-M2.7`
  - `agents.defaults.models` 只保留 `minimax-cn/MiniMax-M2.7`
  - fallback 继续指向 `minimax-cn/MiniMax-M2.7`
- 本次修复后已触发 gateway restart，使新 agentId / 路由 / workspace / MiniMax 配置生效。
- 过程教训：
  1. 对配置问题不能只看 schema，必须看 live config 和真实目录是否存在。
  2. 这类历史错拼写问题常见于“系统能跑但一路都错着自洽”，不迁目录只改配置会把路由修死。
  3. merge patch 不等于删除；涉及重命名/清理残留时，必须显式验证旧值是否真的消失。
