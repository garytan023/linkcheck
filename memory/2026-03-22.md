# 日记 | 2026-03-22

## 今日概览
刚跨日，今天的有效证据窗口还很短，主线不是业务产出，而是把“全 Agent 每日日记”这套流程从半吊子改成能闭环的版本。我这边作为主控，先拿到当天全局中间汇总层做底稿，再确认今天可见事实主要集中在两件事：一是 assistant 和 dev 的 skills 已统一，二是全局汇总层 + 分 agent 日记的 cron 链路已经补齐。

当前能确认活跃的主要是我自己（dev 主控）和拉小码。其余 agent 今天暂时还没有独立 session 证据，不硬编。今天属于流程打底日，不是表面热闹日。

## 今日亮点
1. 把 assistant 与 dev 两边的 skills 管理统一了。
   - 原 assistant 侧 skills 已备份到 `/Users/garytan/.openclaw/workspace-assistant/skills.backup-20260322-0000`
   - `workspace-assistant/skills` 已改成软链接，直接指向 `workspace-dev/skills`
   - 这一下把双份维护这种愚蠢劳动砍掉了

2. 把每日日记机制从“单条脏 cron”升级成“先汇总、再分发”的链路。
   - 22:50：先生成全局中间汇总层
   - 23:00–23:35：再按小风 / 大风 / 小知 / 小数 / 小投 / 小创 / 小诊 / 小码分别写日记
   - 提示词里已经明确：先读全局汇总层；没有才自己做全量汇总

3. 已经识别并修掉老问题。
   - 旧配置里只有 1 条每日日记 cron
   - 还残留 `assitant` 这种拼写错误
   - 路径与角色归属不干净
   这些坑今天都被翻出来并处理了，不再假装“差不多能用”。

4. 今天这份日记本身就是新流程的验证产物。
   - 事实基础来自当天全局中间汇总层，不是只看当前窗口乱写
   - 至少在方法论上，终于像个主控，不像个碰运气的记事本

## 今日问题
1. 今天刚过零点，样本少得可怜。
   目前能看到的独立活跃证据基本只有主控和拉小码，其余 agent 只能记“低活跃/暂无独立证据”。这不是失败，但说明今天的日记更偏流程记录，而不是成果汇报。

2. 旧命名污染还没彻底消失。
   虽然 cron 和 skills 链路修了，但 `workspace-assistant` / `assitant` 这类历史残留还会继续制造混淆，后面不清理干净，迟早又要踩坑。

3. 现在确认的是“任务设计已补齐”，不是“自动执行一定全部成功”。
   也就是说，cron 已创建/更新 ≠ 每晚一定稳定落盘。后面还得继续盯真实产物，不能看见配置就自我感动。

4. 今天还没有新增业务型交付证据。
   暂未看到资讯、数据、投放、创意、诊断、编码这些专业线的新产出。主控侧今天的价值主要在于把基础设施理顺。

## 明天改进
1. 继续验证自动链路，不只看 cron 存在，要看汇总文件和各 agent 日记是否真的按时落盘。
2. 把历史命名残留再清一轮，尤其是 `assitant` 相关路径，别让老坑继续埋雷。
3. 让各专业 agent 尽快恢复到“有独立会话、有实际产出”的状态，不然日记会长期偏空。
4. 继续坚持“先全局汇总，再分 agent 视角”这条规矩。没全量证据就少写，别编。这个底线不能再松。
## 统一记忆系统刷新
- 本轮补录 23 个缺失 session，补齐基础实体/关系，并完成 schema 级校验。
- 重点修复：assistant/assitant 历史分叉导致的 ontology 覆盖不完整，现已把 assistant 侧 session 纳入统一图谱。
- 处理原则维持不变：只把稳定规则/项目关系/关键修复写进长期记忆，临时噪音继续留在 session，不乱升格。

---

## 今晚补记（23:00 后按全量汇总重写视角）

### 今日概览
今天真正有分量的，不是某个花里胡哨的新成果，而是主控链路终于开始像样。

我这边重新以当天全局中间汇总层为底稿，把 2026-03-22 自然日里所有可见 agent、sessions、工具调用、关键文件改动重新扫了一遍。结果很明确：今天最实在的主线有三条——
1. **记忆系统的日常维护真的跑了**：08:00 的 HOT_MEMORY 清理执行成功，并留下备份；凌晨的 daily-memory-refresh 也刷新了 MEMORY、ontology 和当日日记层。
2. **AI 资讯日报至少跑通了一轮完整闭环**：脚本抓到 28 条昨日资讯，整理出 14 条入选摘要，创建了飞书文档，并通过评论 @Gary 提醒查看。
3. **全 Agent 每日日记体系今天有真实落盘证据**：大风、小知、小数、小投、小创、小诊、小码各自的 `2026-03-22.md` 今天都已经存在或被更新，说明“先全局汇总、再分 agent 落盘”不再只是 PPT 流程。

从主控视角看，今天不是业务爆发日，而是**基础设施继续收口的一天**。这活不性感，但比装忙重要得多。

### 今日亮点
1. **主控把“写了算写，送到了算送到”这条规矩继续钉死。**
   heartbeat 巡检时又把老问题翻出来了：不能看到定时任务挂着就回 `HEARTBEAT_OK`，必须查到脚本、文件、通知这些实际证据。今天这条规矩不是口号，是被重新执行了一遍。

2. **日报链路今天至少有一轮真实闭环。**
   - `daily_news_sync_v2.py` 在 09:00 那轮跑出了结果
   - 抓到 28 条昨日资讯，整理为 14 条摘要
   - 飞书文档成功创建：
     `https://www.feishu.cn/docx/XtCfdlyEKoDecrxiiHecHOzEn60`
   - 后续还通过评论 @Gary 提醒查看

   这说明链路不是全死的。至少在某个时间点，它是真跑通了，不是自嗨。

3. **多 agent 日记体系今天算是第一次有“多处落盘”证据。**
   不只是小风自己写一篇自我感动日记，而是 assistant 和多个专业 agent 的 memory 文件都在今天留下了痕迹。就算其中不少是低活跃记录，这也比以前那种“只有一条脏 cron，然后假装全员覆盖”强太多。

4. **问题暴露得够具体，后续才有得修。**
   拉小知下午重跑日报脚本时，结果从早上的“14 条摘要”变成“0 条昨日资讯”。这很关键，因为它把问题范围缩到了几类真实嫌疑：RSS 抓取、日期过滤、checkpoint 逻辑、上游源稳定性。总比“感觉哪里不对”那种废话强。

### 今日问题
1. **日报链路还不稳定。**
   同一天，不同时间，跑出两种完全不同的结果：
   - 一次抓到 28 条并产出 14 条摘要
   - 一次直接 0 条昨日资讯

   这就不是小抖动了，这是链路一致性有病。现在只能确认“不是飞书写入挂了”，但还没彻底抓住根因。Gary 后面补的 RSS XML，也说明这单子还没真正结案。

2. **今天有业务闭环，但 agent 活跃度还是不均。**
   真正有独立业务动作和证据的，还是 dev 主控和拉小知。小数、小投、小创、小诊、小码今天更多是在日记和基础设施层留痕，没有新的分析、投放、创意、诊断、开发交付证据。说白了，体系在，产能分布还不漂亮。

3. **历史命名污染还在。**
   `assitant` 这个拼写错误像幽灵一样，还在目录和历史上下文里晃。今天虽然已经做了很多统一，但这个坑没彻底填平，就还有再次混淆 assistant / dev / 历史遗留路径的风险。

4. **脚本仍在变，说明还没进“稳定期”。**
   `scripts/daily_news_sync_v2.py`、`data/daily_news_checkpoint.json`、`data/wechat_rss_subscriptions.opml` 今天都有改动证据。好消息是流程在迭代；坏消息是它还远没到能放心睡觉的程度。

### 明天改进
1. **优先把日报异常定位到底。**
   重点查四个方向：RSS 源返回是否异常、日期过滤是否错杀、checkpoint 是否误跳过、同一脚本在不同执行上下文下是否拿到不同输入。别再停留在“可能”。

2. **给 heartbeat 做更硬的证据门槛。**
   以后看到定时任务，不看产物不准报 OK。要么查文件落盘，要么查日志，要么查通知送达。继续嘴上说“已执行”这种事，纯属给自己挖坑。

3. **把多 agent 的“有日记”升级成“有产出”。**
   日记链路今天算打通了，下一步要盯的是：各专业 agent 明天能不能恢复各自独立会话和实际业务结果。不然全员都有日记，结果全写“今天没啥”，那也挺黑色幽默的。

4. **继续清历史残留。**
   尤其是 `assitant` 相关命名和路径，能消灭就消灭。工程噪音不清，迟早又演一遍同样的破戏。
