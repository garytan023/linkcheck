# 日记 | 2026-04-03

> 拉小风视角 · append-only
> 首次写入时间：2026-04-03 03:00+ (Asia/Shanghai)

---

## 记忆系统每日整理补记

- 基于 `memory/daily-rollups/2026-04-02.md` 与 `memory/2026-04-02.md` 做增量整理。
- 确认一个高优先级系统结论：**主会话能跑，不代表 isolated session / cron 能跑**；MiniMax-M2.7 在 isolated session 中会直接报 `token plan not supported`，导致 2026-04-02 多个 cron 失败。
- 确认另一个高优先级风险：**飞书消息存在重复入队/去重失效迹象**，主会话曾被数百条重复消息淹没，造成空转。
- 这两项应视为当前 HOT 层问题，先修一致性和稳定性，再谈自动化扩张。
- 本轮整理未重跑先前被审批超时拒绝的外部扫描命令，避免伪造“已扫描成功”。
# 日记 | 2026-04-03

> 拉小风视角 · append-only
> 首次写入时间：2026-04-03 03:00+ (Asia/Shanghai)

---

## 记忆系统每日整理补记

- 基于 `memory/daily-rollups/2026-04-02.md` 与 `memory/2026-04-02.md` 做增量整理。
- 确认一个高优先级系统结论：**主会话能跑，不代表 isolated session / cron 能跑**；MiniMax-M2.7 在 isolated session 中会直接报 `token plan not supported`，导致 2026-04-02 多个 cron 失败。
- 确认另一个高优先级风险：**飞书消息存在重复入队/去重失效迹象**，主会话曾被数百条重复消息淹没，造成空转。
- 这两项应视为当前 HOT 层问题，先修一致性和稳定性，再谈自动化扩张。
- 本轮整理未重跑先前被审批超时拒绝的外部扫描命令，避免伪造"已扫描成功"。

---

## 下午/晚间补充（20:18+）

### Gary 的明确偏好：只回复 ok
- Gary 明确要求：**"只回复 ok"** —— 他希望我以后只回 "ok"，不要多余的话
- 这是明确的用户偏好，必须遵守
- 适用于 Gary 直接发消息的场景（不要加任何额外文字）

### exec.ask 配置无法在线修改
- Gary 问能否关闭 exec 审批（把 `ask: "always"` 改成 `"off"`）
- 结论：**`tools.exec.ask` 是受保护字段**，无法通过 `config.patch` 在线修改
- 解决方案：手动编辑 `~/.openclaw/openclaw.json`，找到 `"ask": "always"` 改为 `"off"`，然后 `openclaw gateway restart`
- 或通过 Web UI（localhost:18789）→ Config 修改

### WhatsApp Gateway 断连
- 上午约 10:42 收到 status 428，WhatsApp gateway 断连
- Gary 全天未提及此事，未询问，视为已知/不紧急
