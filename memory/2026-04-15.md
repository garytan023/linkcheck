# 日记 | 2026-04-15

## 今日概览
今天主线很明确，主要把 OCR、PDF、PPT 这一整条处理链路打通，同时继续推进 karpathy-pkm 的引用式入库方案，并补做 Ontology 的 session 增量刷新。整体看，技术链路前进了一大步，但自动化闭环和 cron 稳定性还是老问题，没资格装作一切顺利。

## 今日亮点
1. **OCR / PDF / PPT 链路正式打通**
   - 本地 PaddleOCR VL client 补上了。
   - `kk_pdf_compile_runner.py` 升级为文本不足时自动走本地 OCR，不再傻等 pending。
   - 明确了“低文本页才触发图片补强”的正式策略，效率和效果都更靠谱。
   - LibreOffice 转 PDF 验证通过，PPTX 到 OCR 的链路终于落地。

2. **karpathy-pkm 引用策略切换完成大半**
   - Obsidian 入库从复制正文改成 reference-only，方向是对的，库不会继续越堆越臃肿。
   - 补了 sync、backfill、compile prep 三个脚本，引用登记和编译前回读原文已经接上。

3. **Ontology 补录完成两批增量刷新**
   - 两批一共补了 59 个 session entity，顺手修了部分历史路径和 agent_id 不一致问题。
   - 图谱目前 326 entities / 993 relations / 0 errors，至少账面是干净的。

## 今日问题
1. **reference compile runner 还没补完**，karpathy-pkm 还差最后一步闭环，不能算真完成。
2. **ISV 目录入库还没清干净**，中文路径编码问题还卡着。
3. **session entity ID 格式不统一** 这事今天还是没动，拖着不是办法。
4. **小风/小诊日记 cron 超时** 还在连续发生，已经不是小毛病，是稳定性债务。
5. **daily-rollup cron 疑似死锁**，说明自动任务体系本身也需要排查。

## 明天改进
1. 先补完 reference compile runner，把 queue → manifest.compiled_summary → wiki 更新彻底串起来。
2. 处理 ISV 中文路径编码，别再让入库卡在这种低级兼容问题上。
3. 专门排查日记 cron 超时和 daily-rollup 死锁，先定位再修，不要再靠运气。
4. 把 PPT/PDF/OCR 统一入口脚本补出来，减少手工判断和分叉流程。
5. 开始推进 session entity ID 统一，别等到截止日前临时救火。
